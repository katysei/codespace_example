name:  01 Building Blocks
on: push
jobs: 
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: echo
              run: echo "Hello World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: fail
              run:  echo "failed!"
            - name: exit
              run: | 
               echo "failed"
               exit -1
            - name: bye
              run: echo "goodbye"